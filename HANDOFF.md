# Uber Routes for Rabbit R1 — Handoff

_Last updated: 2026-07-19 (added hold-to-delete for saved routes/places on Home — there was previously no way to delete one at all — plus the header icon now flashes green→blue as a "thinking" indicator while a geocode request is in flight, replacing the old floating spinner dot. See §9 onward for everything else from this session: a real on-device feedback pass that found and fixed a genuine crash bug, scheduling removed entirely, a route-preview diagram added to Confirm, and two rounds of visual-polish/design-direction changes. Full chronological detail lives in the numbered sections below rather than in this summary line.)_
_Project root: `C:\Users\travi\Documents\R1-projects\uber-routes\`_
_Git repo: `https://github.com/travisbrant1-cyber/uber-routes` (public). Branches: `master` (active, client-side self-ride), `guest-rides-deeplink` (preserves the deep-link-based guest-ride attempt, parked)._

## 1. Goal
A Rabbit R1 "creation" (240×282 WebView web app) that lets you summon Uber rides by **voice or buttons**, using saved standard routes like *"Take me to Work"*.

Originally required "rides for someone else" too (e.g. *"Take Josh to Work"*, Uber texts Josh). **That's out of scope on `master`** — see §3 for why, and the `guest-rides-deeplink` branch for the parked attempt.

Still-active required features (all working):
- Saved, named routes (Home, Work, unlimited, with aliases). Add/edit/delete **on the R1 itself** (no separate phone page needed anymore).
- Trigger by voice phrase **or** buttons/scroll-wheel.
- An **intake wizard** on the R1 that collects ride details and builds a `m.uber.com` deep link + QR. Always requests **now** — no scheduling (removed, see §8: the deep link genuinely can't carry a pickup time, and a feature that silently didn't do what it displayed was worse than not having it).

## 2. How it works (architecture) — fully client-side
The creation has **no backend dependency**. It is a single static file served from GitHub Pages.

| Folder | File | Role |
|---|---|---|
| `r1/` | `index.html` | The R1 creation source (authoring copy). 240×282, bridge feature-detected. Wizard: **+ NEW RIDE** → Pickup → Where → Confirm → `m.uber.com` deep link + QR. (No When step — removed, see §8.) |
| `r1/` | `qrcode.js` | `qrcode-generator` lib for rendering the ride QR. **Was accidentally deleted from `master` in commit `7ce3012`** even though `index.html` still `<script src="qrcode.js">`s it — restored this session from the initial commit (byte-identical to the copy still live on `gh-pages`, so the live site was never actually broken, only the authoring repo was). |
| `r1/` | `icon.png` | Creation icon (512×512 PNG): a green MTA-style circular bullet with a bold white "U", plus a small yellow accent dot echoing the header's status dot. Referenced by `<link rel="icon">` in `index.html` (browser tab icon when testing) and by `iconUrl` in the install QR's JSON envelope (§6) so the R1 launcher shows it. |
| `setup/` | `index.html` | **ORPHANED.** Old phone-side route manager that talked to the backend. Unused now that the creation manages its own routes via `creationStorage`. Kept in repo per request; safe to delete later. |
| `backend/` | `server.js`, `data.json` | **ORPHANED.** Old Node backend (Nominatim geocode + route storage). No longer called by the creation. Kept in repo per request; safe to delete later. |
| root (gh-pages) | `index.html` | **The file GitHub Pages actually serves.** Published from `r1/index.html` via `git show master:r1/index.html`. |
| root (gh-pages) | `install-qr.png` | Install QR (scannable by the R1 camera). Encodes the R1 **JSON envelope** `{"title","url","description","iconUrl","themeColor"}` — NOT a bare URL. |

**What runs where (no server):**
- **Geocoding** → Nominatim (OpenStreetMap) called **directly from the creation** via `fetch` to `https://nominatim.openstreetmap.org/search?format=jsonv2` (CORS-enabled, no API key). `nominatimGeocode()` / `nominatimReverse()` in `r1/index.html`.
- **Saved routes** → `window.creationStorage.plain` (Base64, per-plugin). `loadRoutes()` / `saveRoutes()`. No server, persists on-device.
- **Ride** → `buildSelfLink()` assembles `https://m.uber.com/looking?pickup=...&drop[i]=...` client-side; `doRequest()` renders the QR. You scan with your phone, Uber opens prefilled, **you tap confirm**.

## 3. Why guest rides were removed (read before resurrecting)
Two failed approaches:
1. **Uber Guest Rides API** (`/v1/guests/trips`) — needs an **Uber for Business/Central account + OAuth app** (never obtained). Removed from `master`.
2. **Deep link + manual picker fallback** — tested on-device, confirmed broken: `m.uber.com` deep links open the mobile web UI (not native app), no prefill, rider-picker unreachable.

Guest-ride code is preserved on **`guest-rides-deeplink`** (nothing lost). Scheduling had the same root cause (deep links can't carry a pickup time) — it was tried as a display-only reminder (the QR would tell you to set the time in Uber yourself) but was removed entirely in §8 once a user relied on it and it "didn't survive": a control that visibly lets you pick a time but silently can't act on it is worse than no control at all.

## 4. This session: the real "PTT doesn't work" root cause, and 3 more bugs found in the same pass
The user reported the visual redesign wasn't quite right and that **the side button (PTT) didn't work for entering addresses**. The previous session's notes (now superseded) had guessed this was a hardware-event-naming mismatch (`sideClick` vs `sideButton`) and shipped two speculative, unverified fixes chasing that theory. **That theory was wrong.** Verified directly against Boondit's own current dev-tools documentation: the *only* raw browser events the platform (and its own official emulator shim, which this creation already includes verbatim) ever dispatches are `scrollUp`, `scrollDown`, `sideClick`, `longPressStart`, `longPressEnd` — there is no raw `sideButton`/`scrollWheel` DOM event; those names only exist inside the separate `r1-create` npm package's own JS wrapper. So the dual-listener commit (`b0393f2`) wasn't wrong to have, just aimed at a problem that didn't exist.

**The actual bug**, found by reproducing the flow directly (ran the real code same-origin, dispatched a real `sideClick` CustomEvent with the address input focused and text typed, then inspected computed styles/DOM state — not guesswork): `geocodeThenPreview()`'s success handler called `renderAddrPreview()` (which builds the "Did you mean" screen's content and registers it as the active selection) but **never called `show('addrPreview')`**. So after typing/saying an address and pressing the button — PTT or a plain tap made zero difference, both were fully wired correctly — the geocode silently succeeded, `pendingStop` got set correctly, but the screen never switched away from "Type the destination." From the user's seat this looks *exactly* like "the button didn't do anything," because visually nothing happened, even though the app had actually moved on internally. **Fixed**: added `show('addrPreview')` right after `renderAddrPreview()` in `geocodeThenPreview()`. Verified the full chain three separate ways: (a) direct function calls, (b) a dispatched `sideClick` CustomEvent with the input focused exactly as hardware would leave it, (c) real mouse clicks through the actual UI, typed text → tap Search → "Did you mean" now appears → tap Add stop → back on Where with the stop added.

Three more real, unrelated bugs turned up doing this review, all now fixed in `r1/index.html`:
1. **Long addresses overflowed the "Did you mean" screen entirely**, pushing the Retry row completely off the fixed 282px viewport (confirmed via `getBoundingClientRect()`: Retry's box started at y=283.7, one pixel past the visible area — not reachable by touch at all). No cap existed on `#addrPreviewText`, unlike every other screen in this app which bounds its text. Fixed with `-webkit-line-clamp:3` + `max-height:60px` (matches the WebView's Chromium engine, so this is safe on-device).
2. **The marquee horizontal-scroll (for long destination text on the Confirm and QR screens) silently never engaged.** `fitMarquee()` was called *before* `show()` in both `goConfirm()` and `doRequest()`, so it measured `scrollWidth` on a `display:none` element (always reads 0) — the overflow check always failed, so `--sd` and the `.scroll` class never got set, and long text was just permanently truncated with the CSS `overflow:hidden` on the wrapper cutting it off, no scroll, no ellipsis, no way to ever read the rest. Fixed by moving both `fitMarquee(...)` calls to *after* their `show(...)` call. Verified: a long address (`scrollWidth` 1071px vs 220px `clientWidth`) now correctly gets `.scroll` + `--sd:-851px` applied.
3. **A previously-fixed mislabeling bug came back.** The Where screen's first stop was labeled "pickup →" (`i===0 ? 'pickup →' : 'stop '+(i+1)`) — conflating the destination-stops list with the separate, dedicated Pickup step earlier in the wizard. This exact confusion was diagnosed and fixed earlier in this project's history (renamed to "Stop 1" back then) and silently reappeared in the MTA rewrite. Reverted to consistent "stop N" labeling for every stop including the first.

Also removed two empty, permanently-unpopulated `<div class="prog">` containers on the `typeAddr` and `addrPreview` screens (they're not part of the 4-step `WIZ` array so `setProg()` never filled them) — this was leaving an ~18px dead blank gap right under the header on both screens, which is likely part of what read as visually "not quite right." Reclaimed that space for content instead.

**Testing method note:** none of this needed a physical R1 — it was fully reproducible by (a) restoring `r1/qrcode.js` and serving the repo locally, (b) same-origin `javascript_exec` to call internal functions/dispatch real `CustomEvent`s and inspect live DOM/computed-style state, and (c) real mouse clicks through the actual rendered UI at 240×282. A separate cross-origin iframe harness mimicking the Boondit emulator's exact `postMessage` protocol also confirmed the scroll/side-click relay itself works correctly — the bug was never in the event-delivery layer.

## 5. This session: focused visual-polish pass, and 2 more bugs found while verifying it
After the bug-fix pass in §4, the user said the MTA subway UI was "close" but still read as flat and static. Rather than a deeper redesign, this was a targeted polish pass within the existing aesthetic (user explicitly chose "focused polish" over redesign, and to skip sound effects for now). All changes are in `r1/index.html` only — no new screens, no architecture changes.

**What changed:**
1. **Screen-slide transitions.** `show(id)` used to hard-swap `display:none`/`flex` with no motion. Added a `SCREEN_ORDER` array and a `screenDir()` helper that decides `fwd`/`back` by comparing wizard-step position (with special handling for the `typeAddr`/`addrPreview` branch screens, which compare against whichever step they branched from — `pickup` or `where`, via `addrTarget`). The incoming screen now slides in from the right (fwd) or left (back) while the outgoing one slides out the opposite way, `transform`/`opacity` only, ~180ms, cleaned up via `#view`'s existing `overflow:hidden` (already clips correctly, no extra CSS needed there).
2. **Progress-line "train dot."** `setProg()` now adds a small dot that slides from the previous step's position to the current one every time it runs (`transform`-only, ~320ms, via a double-`requestAnimationFrame` to force the "from" state to paint first).
3. **Selection depth.** `.row.sel` now transitions (120ms) instead of snapping, and glows with the row's own bullet color via a `--rc` CSS custom property set per-row in `station()`.
4. **Faint background texture.** A ~3.5%-opacity diagonal hatch (`repeating-linear-gradient`) on `#app`, static CSS only, no runtime cost.
5. **Typographic hierarchy.** Row subtitles (`.meta .s`) no longer inherit the global bold weight, so titles now actually read as the primary text.
6. **Home dead-space treatment.** A `.lineTrail` element (3 progressively-fainter dots on a fading vertical line) appended after the last Home row, extending the metro-line motif into the empty space below a short route list instead of stopping dead.

**2 real bugs found during the verification pass** (not part of the plan — found by actually exercising the app end-to-end after the changes landed):
- **`show('when')` was never called anywhere in the codebase.** `goWhen()` built the When screen's content and selection model but never switched to it — tapping "Continue" on the Where screen silently did nothing visible, identical in shape to the `show('addrPreview')` bug from §4. Fixed by adding `show('when')` at the end of `goWhen()`.
- **A double-`show()` race in `addrConfirm()`** (`if(addrTarget==='pickup') goWhere(); else renderStops(); show('where');` — the trailing `show('where')` ran unconditionally due to missing braces, so the pickup-confirm path called `show('where')` twice in the same tick). Harmless before this session (idempotent), but it canceled the new slide-transition mid-flight. Fixed the branching to only call `show('where')` once. While fixing this, also found and closed a related **general race**: if `show()` is called again for a screen before its previous transition's `requestAnimationFrame`/cleanup `setTimeout` fires, the stale callback could later overwrite the new state, leaving two screens visible at once. Fixed by storing the pending rAF/timeout handle on each element (`el._pendingRAF` / `el._pendingCleanup`) and canceling it at the top of every `show()` call before scheduling new work — matters because this codebase already documented that the R1 hardware can send duplicate rapid events, so back-to-back `show()` calls are a real scenario, not just a test artifact.

**Testing method:** same as §4 — no physical device, `npx serve` locally (this time serving `r1/` directly as the server root via `.claude/launch.json`, sidestepping the trailing-slash gotcha entirely), `javascript_exec` calling internal functions directly and inspecting live DOM/style state, real clicks through the rendered UI at 240×282, and `getBoundingClientRect()`/console checks after every change. Verified: the original PTT fix still works post-transition, long-address line-clamp still holds, marquee `--sd` still engages correctly, "stop 1" labeling still correct, forward/back slide direction is correct in every wizard branch including the address-entry sub-flow, and rapid back-to-back navigation no longer leaves two screens visible.

**Not yet done from this pass:** nothing pushed to `master`/`gh-pages` — per the established checkpoint in this project, visual-polish work (like the §4 bug fixes before it) waits for explicit user review before committing/deploying.

## 6. This session: the save-route flow was actually broken, plus a real icon
The user tried saving a route on-device and reported it "didn't stick" after entering the name, and asked whether saved places share storage with routes (they do — see §2/§4, `savePrompt()` always writes into the same `routes` array regardless of pickup-only vs. destination saves). While reproducing this, found **two distinct, compounding bugs** in the inline prompt modal (`r1/index.html`), both now fixed:

1. **The on-screen tip told you to do the wrong thing.** `#promptModal`'s tip read `HOLD ● CONFIRM · SWIPE ⟶ CANCEL` — but `longPressStart` on that screen is wired to *cancel* (`show(promptBackTo)`, discarding whatever you typed), while a plain tap (`sideClick`) or left-swipe actually confirms. So a user following the tip's own instruction — holding the button to "confirm" — was silently canceling every time. Fixed the tip to `● CONFIRM · SWIPE ⟶ CANCEL`.
2. **Even with the correct tap, `confirmPrompt()` had a logic bug**: `if(val && cb) cb(val); else if(back) show(back);` — this only invoked the callback when the typed value was non-empty. The Aliases step is explicitly optional ("Other names? (comma-separated, optional)"), so confirming it blank (the common case) never called the save callback at all — it just silently re-displayed the modal (since `promptBackTo` for that step is `'promptModal'` itself), looking exactly like "nothing happened." Fixed by always invoking `cb` when one exists; each callback already knows how to handle an empty answer for its own field (the name callback bails to `goPickup()`/`goWhere()` on empty name; the aliases callback just proceeds with an empty aliases array).

Verified both fixes together end-to-end for **both** save entry points (`savePrompt('pickup')` from the pickup address-preview screen, and `savePrompt('stop')` from a destination/the QR screen's "Save route" row): typed a name, confirmed via a real dispatched `sideClick` (not hold), left Aliases blank, confirmed again — the route now correctly lands in the `routes` array and appears on Home. Also renamed the prompt title **"Name this place" → "Name this route"** per the user's request, since there's only one underlying concept (a route, optionally missing a pickup or dropoff) — "place" was a holdover from before that was unified.

**Also added a creation icon** (`r1/icon.png`, 512×512): a green MTA-style circular "station bullet" with a bold white "U", plus a small yellow dot in the corner echoing the header's own status-dot accent — generated locally with Pillow, not sourced from anywhere external. Wired up two ways:
- `<link rel="icon" type="image/png" href="icon.png">` added to `r1/index.html` (shows in a browser tab when testing; cosmetic only on-device, since the R1 WebView has no browser chrome).
- `install-qr.png` regenerated with the same JSON envelope as before, now with `iconUrl` filled in: `"https://travisbrant1-cyber.github.io/uber-routes/icon.png"` (previously `""`). `themeColor` was left as-is (`#FE5000`) — it predates this session and wasn't part of what was asked; worth a look sometime since it doesn't match the MTA-green icon or the app's own yellow/black chrome, but that's a separate call. Decoded the regenerated QR back with OpenCV to confirm it round-trips correctly before saving it over the old file.

**Publish reminder:** the gh-pages publish step (§17) currently only copies `index.html` (and, per the earlier `qrcode.js` reminder, should also copy that if it changes). Now `icon.png` needs the same treatment the first time this ships — `git show master:r1/icon.png > icon.png` while on `gh-pages` — otherwise the install QR's `iconUrl` will 404 even though everything else works.

## 7. This session: a third save-flow bug (real keyboard Enter), and the scheduling limitation re-confirmed
After §6's fixes, the user reported the save still didn't stick when testing in a browser preview, and separately that a scheduled ride's deep link "didn't survive." Two different findings:

**A third real bug, same family as §6:** the shared `document`-level `keydown` handler has always special-cased `#addrInput` — it explicitly skips its own Enter-handling logic when that field is focused, because `addrInput` has its own dedicated `keydown` listener (this exclusion is what an earlier session's Enter-key fix for address entry relied on). `#promptInput` (the save-route name/alias field) had **no such exclusion and no dedicated listener of its own**. So focusing that field, typing a name, and pressing Enter — the natural way to submit a text field in any browser — fell through to the generic handler's last branch, `else if(selItems.length) activate();`, which activates whatever was left in the *stale* `selItems` array from a completely different screen, never calling `confirmPrompt()` at all. This is almost certainly what "still doesn't appear to save in the preview" was: the side-button/tap path (fixed in §6) works, but Enter — what you'd naturally reach for on a keyboard — didn't. Fixed by adding a dedicated `#promptInput` keydown listener (mirroring `addrInput`'s own pattern exactly) and excluding `promptInput` from the shared handler the same way `addrInput` already is. (Initially added a `promptModal` case to the shared handler *instead*, but that double-fired `confirmPrompt()` on every Enter — one call from the dedicated listener, a second from the event bubbling up to `document` — with the second call reading an already-cleared input value; caught this in testing and switched to the exclusion-based fix instead, which cannot double-fire.)

**The scheduling report is the known limitation, not a new regression.** Verified directly: picking "+30 min" on the When screen correctly sets `intake.when`, and it correctly displays on both the Confirm screen (`When: 7/19/2026, 7:29:36 PM`) and the QR screen (`Scheduled: 7/19/2026, 7:29:36 PM (set in Uber)`) — so the display/reminder path has no bug. But `buildSelfLink()` — checked the actual generated URL — only ever emits `pickup` and `drop[i]` parameters; there is no code path anywhere that could attach a time, because `m.uber.com/looking` has no schedule parameter at all (confirmed earlier this project directly against Uber's own developer docs). This is exactly the limitation already documented in §3: the only way to get a *real* scheduled ride requested automatically is Uber's Guest Rides / Business API, which this project explicitly parked earlier for lack of a Business account. Nothing to fix here without that access — the app is already doing the only thing it can (showing the chosen time prominently as a "set this manually" reminder on both screens that lead to the QR).

## 8. This session: scheduling removed entirely
After §7 re-confirmed that scheduling was a hard platform limitation rather than a bug, the user's call was simple: remove references to it. Rather than just hiding the display text, took this as a chance to strip the whole feature cleanly out of `r1/index.html`, since a "When" step that lets you pick a time and then can't act on it is misleading — the report that a scheduled ride "didn't survive" is exactly the confusion this was causing.

**Removed:**
- The **When** screen entirely (HTML `<div id="when">`, its `<div class="prog" id="progWhen">`, `whenList`).
- `goWhen()`, `tomorrowAt()`, `sayTime()`, `parseTime()` — all gone.
- `intake.when` and every reference to it: the "When: ..." line on Confirm, the "scheduled · press ●" vs "press ●" toggle (Confirm's Request row now just always reads "press ●" via static HTML, no JS needed), the "Scheduled: ... (set in Uber)" note on the QR screen (now always "Ride now").
- The voice-phrase LLM prompt no longer asks for or parses a time (`{"route":string|null}` instead of also `"when"`) — a matched route now always jumps straight to the QR (`quickSelf(r)`) instead of conditionally routing through Confirm first.
- The `expect==='time'` branch in `onPluginMessage` (was reachable only from the now-removed "Say a time" button).

**Wizard is now 3 steps, not 4:** `WIZ` and `SCREEN_ORDER` no longer include `'when'`. `setProg()`'s progress bar rebuilds for 3 dots (`Pickup`/`Where`/`Go`) instead of 4. Navigation updated to match: `renderStops()`'s "Continue" row and `goForward()` from `where` now call `goConfirm()` directly (previously `goWhen()`); `goBack()`/`goForward()` from `confirm` now go straight to/from `where`; the `longPressStart` "cancel to Home" screen list and the `show()` screen-list both drop `'when'`.

Verified end-to-end after the removal: Pickup → Where → Confirm → QR walks straight through with no When step, the progress bar correctly shows 3 dots, Confirm/QR text no longer mentions a time, `goBack()` from Confirm lands on Where, the voice-phrase shortcut still jumps straight to a QR for a matched route, and no console errors across the full run.

Guest rides remain separately blocked pending Uber for Business API access (§3) — that's a different, still-open door; scheduling had no such door (Uber's public deep link has no schedule parameter at all), which is why it was removed instead of left parked.

## 9. First real on-device feedback pass
The user finally tried this on the actual R1 hardware and came back with a long list. One of the reports turned out to be a genuine, confirmable crash bug; the rest are simplifications and hardening that can't be fully verified without the physical device, so each is called out as such below.

**The real bug — found and fixed, confirmed via reproduction:** "After a ride QR code is displayed and you get back to home screen, only Speak a ride shows. The saved routes don't appear." This was never a storage/persistence problem (routes were being saved correctly the whole time) — `renderHome()` unconditionally read `r.dropoff.addr` for every saved route, with no null check. Any **pickup-only** saved route (saved from the Pickup screen, e.g. via "Save pickup" — its `dropoff` field is legitimately `null`) throws a `TypeError` partway through the `routes.forEach` loop, which **aborts the rest of `renderHome()` immediately** — including every route after the broken one, the "New ride" button, the line-trail decoration, and the `registerList()` call that wires up scroll/PTT selection on Home. Since "Say a ride" is appended *before* the loop runs, it's the only thing left standing, which is exactly the symptom reported. Reproduced directly (`routes = [{name:'X', dropoff:null, ...}]; show('home')` throws `Cannot read properties of null (reading 'addr')`), fixed by skipping routes with no `dropoff.lat` in the three places that assumed every route has one: `renderHome()`, `pickRoute()` (the Where screen's "Saved place" picker), and `rideHome()` (guards a "Home"-named route with no dropoff instead of crashing). Verified: a pickup-only route no longer breaks Home (skipped there, correctly still offered as a saved pickup on the Pickup screen instead), and a destination route sitting right next to it in storage renders fine.

**Simplifications, as requested:**
- **Removed route aliases entirely.** `savePrompt()` no longer asks a second "Other names?" question — one name, one save. `routeMatchesName`/`findBestRoute`/the voice-phrase LLM prompt all simplified to match by name only. (Old saved routes with a leftover `aliases` array from before are harmless — just unused now.)
- **Removed the GPS "Current location" option** from the Pickup screen (`useCurrentLocation()` and the now-unused `nominatimReverse()` deleted). It never worked on-device — the R1 WebView doesn't surface a geolocation permission prompt — and "Skip · my location" already achieves the same outcome by leaving pickup unset, which makes Uber use the phone's own current location when the link opens. Depending on the Uber app for this instead of trying to do it ourselves.
- **Address display simplified to house number + street.** `goConfirm()` and `doRequest()` now put the destination through `shortAddr()` (already used elsewhere in the app) instead of the raw Nominatim `display_name`, so Confirm/QR now show e.g. "6041, Wallace Road Extension" instead of the full "…, Pine Township, Allegheny County, Pennsylvania, 15090, United States". Also fixed a small pre-existing bug in `shortAddr()` itself: it wasn't trimming the comma-split parts, so results like "1106,  Summit Street" had a stray double space — now trimmed. (Left the "Did you mean" screen showing the *full* address on purpose — that one's job is letting you verify a fuzzy geocode actually matched the right city/state, where more detail genuinely helps.)

**Hardening for on-device reliability (best-effort — could not be fully verified without the physical R1):**
- **Swipe navigation.** Was pointer-events-only (`pointerdown`/`pointerup`), which is a plausible cause of unreliable swipes in an Android WebView (touch input doesn't always cleanly map to synthetic PointerEvents depending on WebView version/config). Added real `touchstart`/`touchend` listeners as the primary path, with `touch-action:none` on `#view` so the system doesn't intercept/cancel the gesture first, and kept pointer events only as a desktop-testing fallback (explicitly skipped when `e.pointerType==='touch'`, so a real touch can never double-fire through both listeners). Verified in-browser that both the touch-event path and the pointer-event path independently trigger `goBack()`/`goForward()` correctly and don't double-fire — but this is exactly the kind of fix that can look right in any browser and still behave differently on the actual WebView, so it needs a real on-device swipe test.
- **PTT / long-press reliability, particularly "spotty" long-press-to-Home.** Added the same debounce `sideClick`/`sideButton` already had (`_clickGuard`) to `longPressStart` too, and made a long-press "poison" the click guard (`_lastClick=now`) so a trailing `sideClick`/`sideButton` the host might fire for the same physical press-and-release can't immediately re-trigger navigation right after the long-press already acted. This is a reasonable hardening given the failure mode (two events for one physical gesture, a pattern this codebase has already had to guard against once for the side button), but genuinely can't be confirmed fixed without hardware — flagging honestly rather than claiming certainty.

**Visual changes:**
- **Removed the header status dot** (`#modeDot`) entirely — decorative, unused, not worth the space.
- **Redesigned the header** to read as more than a plain text bar: added a small green circular "U" badge (echoing the app icon) to the left of the wordmark, bumped the height slightly (22px → 27px), tightened letter-spacing, and added a subtle hairline under the gold border for depth.
- **Background texture upgraded** from a plain diagonal hatch to a layered street-grid plus three faint diagonal "subway line" streaks at very low opacity (3.5–5%). **Superseded almost immediately — see §10:** the user found this too subtle to actually see, so it was replaced with a bolder, animated SVG version.
- **Fixed the progress-bar dot centering.** The train dot's `translateX` math aligned its own *left edge* to the target step-dot's center instead of its own center, leaving it visibly off by half its own width (2.5px). Fixed by subtracting the dot's own half-width from the target offset. Verified via `getBoundingClientRect()`: dot center and step-dot center now land on the exact same pixel.
- **More left padding next to bullets/icons.** `.row` padding-left bumped from 38px to 45px (and the Confirm screen's Request row, which hardcodes the same padding inline, updated to match) — more breathing room between the icon and the title/subtitle text.

## 10. Background redone as an animated SVG (§9's CSS-gradient texture was invisible)
Immediate follow-up to §9: the user said the layered-gradient background from §9 was "really hard to see, if not impossible," and asked whether an animated SVG was possible instead of a static texture. It was — replaced entirely.

**What's there now:** an `<svg id="bgLines">` sits as the first child of `#app` (so it paints behind `header`/`#view` in normal DOM-order stacking, no z-index tricks needed), containing 3 curved paths in MTA palette colors (blue `#0039A6`, yellow `#FCCC0A`, green `#00933C`) crossing the 240×282 canvas like a small transit-map diagram, each stroked at 20–24% opacity (visibly bolder than §9's 3.5–5%, per the "hard to see" feedback). One small glowing dot per line (`<circle>` + `filter:drop-shadow`) travels along its path continuously via native SVG `<animateMotion>` — no JS, no `requestAnimationFrame` loop; the browser's own SMIL engine drives it.

**Why this animation approach specifically**, given this project's established rule of only animating `transform`/`opacity` on the R1's weak SoC: `animateMotion` only touches 3 tiny circles (a few pixels each, not full-canvas redraws), matching the spirit of that rule even though strictly speaking it's a continuous (indefinite) animation rather than a one-shot. As a belt-and-suspenders measure given it *is* continuous, added a `visibilitychange` listener that calls the SVG's native `pauseAnimations()`/`unpauseAnimations()` when the app is backgrounded — so it's not spending cycles animating an invisible screen. Confirmed both methods exist on the SVG DOM element in this browser (Chromium-based, same engine as the R1 WebView).

The QR code itself sits on its own opaque white `#qr` background box, so the moving lines never show through it or risk hurting scannability — confirmed visually.

**Verified:** loaded fresh, no console errors; screenshotted twice ~2s apart and confirmed all three dots visibly advanced along their paths; checked Home, Confirm, and QR screens all render the animation correctly behind their content; confirmed `pauseAnimations`/`unpauseAnimations` are real functions on the element. **Not verified:** actual animation smoothness/CPU cost on the physical R1 SoC — this is exactly the kind of thing that can look fine in any Chromium-based browser and still need a second look on real hardware, so worth keeping an eye on during the on-device pass this project already owes from §9.

## 11. Route preview on the Confirm screen (a stylized diagram, not a literal map)
User asked: "is it possible to show a map of the route before the QR code is generated?" Two real options exist here and they're genuinely different builds, so this one got an explicit choice up front (via `AskUserQuestion`) rather than guessed at: (a) a real map image stitched from OpenStreetMap tiles — looks like an actual map, but needs tile-coordinate math, image stitching, mandatory "© OpenStreetMap contributors" attribution, and leans on OSM's tile servers in a way their usage policy asks apps not to do beyond light/personal use; or (b) a stylized line diagram in the app's own subway-map visual language, plotted from the ride's real coordinates but drawn schematically rather than as cartography — no external service, no key, no attribution requirement, instant. **User picked (b).**

**What it is:** on the Confirm screen, between the destination text and the "From: ..." line, a small SVG (`#routeMap`, 200×76 viewBox) now draws the actual shape of the trip: a green dot for pickup, small grey dots for any intermediate stops, a blue dot for the final destination, connected by a white line — station-bullet styling reused straight from the rest of the app. `renderRouteMap()` (new function, called from `goConfirm()`) does the plotting:
- Projects each point's real `lat`/`lng` onto the 2D SVG canvas with a simple equirectangular transform (`x=lng, y=-lat` — fine at city scale, no need for anything fancier over a few miles), so the diagram's shape and relative direction are real, just not drawn to cartographic scale or actual road paths — the same spirit as a real subway map, which is deliberately schematic rather than geographically accurate.
- Preserves the true aspect ratio of the bounding box across all points (scaled to fit, not stretched), so a mostly-north-south trip still reads as mostly-vertical, etc.
- **No pickup set** (GPS unavailable on-device, or the wizard was skipped): rather than fabricate a fake starting coordinate, it anchors an unfilled, dashed "unknown start" point near the first stop and draws the connecting line dashed too — visually honest about what isn't actually known, instead of implying false precision.
- Multi-stop trips draw the full polyline through every stop in order, with only the very first and very last points getting the larger pickup/dest styling.

**Verified in-browser** across three cases: a normal 2-point (pickup + destination) trip, the no-pickup placeholder-start case, and a 4-point (pickup + 2 stops + destination) trip — all three plot correctly with no console errors, and the diagram comfortably fits the Confirm screen's existing vertical space (bumped from an initial 56px-tall draft to 76px once measuring confirmed there was room, without pushing the Request button off-screen).

## 12. Route-map city labels, plus a CSS visual-polish batch (mostly adopted from an outside review)
Two follow-ups landed together. First, the user said §11's route diagram "looks good" but asked for major cities/points of reference so it reads as a real place, not abstract art. Second, they'd asked another model for visual-maturity suggestions and wanted a second opinion before applying them.

**City/place labels on the route map:** rather than hardcode a city database (which would tie this generic app to one region) or add another geocoding round-trip, this reuses data the app already fetches. `nominatimGeocode()` was already requesting `addressdetails=1` from Nominatim but discarding the structured `address` object after pulling `display_name` — now it also extracts `place: a.city || a.town || a.village || a.hamlet || a.suburb || null` and returns it. That `place` field rides along on `pendingStop`, through `addrConfirm()` into `intake.pickup`/`intake.stops[i]`, and into saved routes too (`savePrompt()`, so a route's `pickup`/`dropoff` now carry `place`, and both `pickRoute()` and the Pickup screen's "saved pickup" rows pass it through when a saved route is reused). `renderRouteMap()` now draws a small uppercase label (`<text class="rmLbl">`) next to the pickup and destination dots when a `place` name is available, with the label's `text-anchor` and vertical offset chosen based on the dot's position in the 200×76 canvas so it doesn't clip off the edge. Intermediate stops are deliberately left unlabeled to avoid clutter on multi-stop trips — the point of the labels is orienting the two endpoints, not fully annotating the diagram. Verified: a White Oak → Wexford, PA test route now shows both town names next to their dots, correctly positioned, no console errors.

**The CSS review, and what I actually applied:** the other model's list was good work overall — real understanding of this app's compositor-only-animation constraint, correctly steered away from `backdrop-filter` and SVG `<filter>` elements, and its `#8` (route-map glow via CSS `filter:drop-shadow` instead of an SVG `<filter>`) matches exactly how the background-SVG animation in §10 was already built. Adopted as-given: tighter/brighter `.row.sel` glow, a 60ms `:active` press-scale on rows (real touch/click only — doesn't fire for hardware-driven `.click()` calls via `activate()`, but that's a legitimate gap in coverage, not a bug), header/marquee `text-shadow`, a QR halo, an express-bullet bloom, and the route-map `drop-shadow`s.

Two of the eight got changed rather than applied as-written:
- **Pulsing train dot:** the suggestion animated `box-shadow` size in an infinite keyframe loop. `box-shadow` isn't a compositor-only property — animating it forces a repaint every frame, which runs contrary to this project's own established rule (animate only `transform`/`opacity` on this hardware, from the §5/§10 visual-polish work) precisely because it's *continuous*, not one-shot. There's also a concrete conflict specific to this element: `.trainDot`'s `transform` is already driven by JS for positioning (`setProg()`), so a CSS keyframe animating `transform` too would fight the inline style and visibly break the dot's position. Implemented the same "breathing" feel with an `opacity` keyframe instead (`.7` ↔ `1`, 1.6s loop) — compositor-only, and doesn't touch the property JS needs.
- **Scanline overlay:** skipped. As proposed it's a `#app::after` pseudo-element at `z-index:100`, meaning it paints *above every screen*, including the QR screen — and `#qr`'s own white background wouldn't shield it, since z-index:100 sits above that too. A repeating dark-stripe pattern layered directly over the QR code's white modules is a real scannability risk on a phone camera at a distance, not just an aesthetic call. Didn't build a QR-screen exclusion for this round since it adds real complexity (either a `:has()` selector this project hasn't relied on elsewhere, or a dedicated toggleable element) for an effect that's the least essential of the eight — flagging it here in case it's still wanted with that exclusion built in.

Verified all of the above in-browser: Home, Pickup, Confirm (with the glowing route map), and QR screens all screenshot correctly, the train dot's pulse animates via `opacity` without disturbing its `transform`-driven position (confirmed via `getComputedStyle`), and no console errors anywhere in the pass.

## 13. "Make this feel more grown up" — one restrained accent, quieter dots, monochrome background
Open-ended design question, so this didn't get guessed at blind: asked the user to pick a direction first (`AskUserQuestion`, three concrete options — restrained editorial, premium dark UI, or a desaturated transit map). They didn't pick one outright; instead gave specific, actionable feedback: they like most of the design as-is, but the bright yellow header bar is "throwing it off," the dot elements "feel less premium," and the background could be "upgraded." That's concrete enough to act on directly rather than asking a follow-up.

**The core idea:** one restrained signature accent instead of scattering bright saturated color everywhere as plain decoration. Added `--gold:#C9A458` (a muted brass/gold, defined once on `:root`) and swapped it in wherever color had been used to mean "active/in-progress/notify" rather than to distinguish one thing from another:
- **Header:** removed the `3px solid #FCCC0A` bar entirely, replaced with a plain `1px solid rgba(255,255,255,.12)` hairline. Also dropped a now-redundant second hairline (`header::after`) that existed only to add depth *under* the old bold bar.
- **Train dot + current-step label** (`.trainDot`, `.prog .pl.cur`): swapped bright yellow for `var(--gold)`, and tightened the glow (blur/spread cut roughly in half, pulse range narrowed from `.7↔1` to `.8↔1` opacity) — still reads as "alive," less like a warning light.
- **Toast banner and the loading-spinner dot** (`.toast`, `.loading::after`): also moved to `var(--gold)` — left as bright saturated yellow, these would have clashed with the new restrained palette everywhere else. `.warn` text color too, for the same reason.
- **Route-map pickup/destination dots:** desaturated from saturated MTA primaries (`#00933C` green, `#0039A6` blue) to muted tones (`#5f9e73`, `#5a7bab`) with a softer, tighter glow.
- **Background (`#bgLines`):** the three animated subway-line paths went from competing bright primaries (blue/yellow/green) to three tonal variations of one muted slate-blue family, at slightly lower opacity, with matching muted glow on the traveling dots. Keeps the "alive, moving map" feeling from §10 without reading as a rainbow.

**Deliberately left alone:** the per-route/action color system (`PALETTE`, used to give each saved route and each express action — Save, Continue, New Ride — its own distinguishing color) and all typography/casing. Those weren't what the user flagged, and touching them would have been scope creep beyond "the header bar, the dots, and the background."

Verified in-browser: Pickup, Home, Confirm (route map with the new muted dots and blue-gray background), QR, and a manually-triggered toast all screenshot correctly with the new palette; confirmed via `getComputedStyle`/`getBoundingClientRect` that the toast renders with the correct gold background and position (a couple of my own checks caught its own 2.2s auto-hide firing between round-trips, which is expected behavior, not a bug). No console errors.

## 14. Loading spinner replaced by the header icon itself flashing green→blue
User asked for the app's own icon (the header's green "U" badge) to flash from its normal green to the new blue accent while a request is in flight, instead of a separate loading indicator.

There already *was* a separate spinner (`.loading::after`) — a small gold dot fixed to the exact center of the screen, pulsing via `opacity`, shown/hidden by toggling a `loading` class on `<body>` (already wired into `nominatimGeocode()`'s fetch lifecycle). Rather than run two different "thinking" indicators at once, replaced it outright: added `body.loading header .badge { animation: badgeThink .9s ease-in-out infinite; }` with `@keyframes badgeThink` crossfading the badge's `background` between `#00933C` (its normal green) and a new `--blue:#5a7bab` custom property, then deleted the old floating dot and its keyframes. No JS changes needed — the existing `classList.add('loading')`/`remove('loading')` calls in `nominatimGeocode()` already flip the CSS on and off; only what reacts to that class changed. Also switched the route-map's destination-pin color to reference the same `--blue` variable instead of its own hardcoded hex, so "the new blue" means one consistent color across the app rather than two similar-but-different blues.

Verified: toggling `body.loading` directly in the console confirmed the animation applies/removes correctly and the badge reverts to solid green when cleared; sampled `getComputedStyle(badge).backgroundColor` every 150ms during an active cycle and confirmed the value actually interpolates from green toward blue (not just a class toggle with no visible effect); ran a real `geocodeThenPreview()` call and confirmed `body.loading` and the animation are both live at the moment the fetch is in flight. No console errors.

## 15. Deleting saved routes: there was no way to do this at all
User asked how deletion of saved routes/places was handled, and proposed a specific hold-to-delete gesture if it wasn't. Checked first rather than assuming: grepped for any `routes.splice`/delete path and found none — `routes` could only ever grow (`.push`) or get overwritten by re-saving the same name. There was never any way to remove a saved route or place short of clearing `creationStorage` outside the app entirely. Built exactly what was proposed, scoped to the Home screen's route list (see below for why not elsewhere).

**The gesture, matching the ask precisely:** long-press a saved route on Home to "arm" it — the row turns red (`.row.armDelete`, a deep red background + red-ring box-shadow) and its subtitle changes from the address to "press ● to delete". A subsequent side-button press (or a direct tap on the still-armed row) deletes it; both paths converge on the same `onClick` handler, since the hardware side-button path already works by calling `.click()` on the selected element (`activate()`), so no separate hardware-specific delete branch was needed.

**Canceling is deliberately easy**, since this is a destructive action: long-pressing the *same* armed row again toggles it back off (arm/disarm is a single toggle); scrolling to a different row cancels it too (implemented in `moveSel()`, so a reflexive "step away" instinct works as an undo); and navigating off Home and back always clears any stale armed state (`renderHome()` resets `armedRouteIdx=-1` on every normal entry, only preserving it across the internal re-renders that toggle the arm/disarm visual itself).

**Implementation notes:**
- `registerList(els, keepSel)` gained an optional second parameter — normally selection resets to index 0 on every re-render (needed so a fresh screen always starts at the top), but arming/disarming needs to redraw the list *without* losing the user's current selection, so `renderHome(true)` (used only for the arm-toggle and post-delete re-renders) passes `keepSel` through, clamping the preserved index if the list just shrank by one.
- Each route row is tagged with `dataset.routeIdx` so `longPressStart` can look at whatever's currently selected and know both "is this actually a deletable route row" and "which index in `routes`."
- Deletion itself (`deleteArmedRoute()`) splices the array, re-renders immediately (optimistic UI), then awaits `saveRoutes()` before toasting confirmation — same pattern as the save-route fix earlier in this project, so a delete can't silently fail to persist either.

**Scope decision — Home only, not Pickup's "saved pickup" rows or Where's "Saved place" picker:** those two screens already use `longPressStart` for its original purpose (bail out to Home), and conditionally repurposing that same gesture there — only when the selected row happens to be a route — risked an accidental delete-arm when someone just wanted to escape the screen. Home doesn't have any existing long-press behavior of its own (you're already home), so it was free to claim without any conflict. If deleting from those other views turns out to matter, it needs its own design pass rather than reusing this exact mechanism.

Verified end-to-end in-browser: armed a route via a dispatched `longPressStart` (confirmed `armDelete` class + "press ● to delete" subtitle + the actual red screenshot), confirmed a `sideClick` while armed deletes it and clears `armedRouteIdx`, confirmed the 600ms long-press debounce (from §11) correctly blocks a same-tick second long-press but a properly-spaced one toggles disarm, confirmed scrolling away disarms, confirmed navigating off Home and back always clears stale armed state even when forced directly, and confirmed a normal (non-armed) tap on a route still quick-rides exactly as before. No console errors anywhere in the pass.

## 16. Status
**Working and verified (post-fix):**
- Full self-ride wizard: **+ NEW RIDE** → Pickup (saved / typed / spoken, skippable — no GPS option, §9) → Where (multi-stop, saved / typed / spoken) → Confirm → QR, always for now (no When step — removed, §8). One-tap quick-ride from the Home list. Re-verified end-to-end via real clicks after all fixes above.
- **Confident-match auto-skip:** exact geocodes (building with a house number, or Nominatim importance > 0.6) add instantly without the "Did you mean" screen; fuzzy matches still preview (and now that screen actually displays and never overflows).
- Voice phrase shortcut (`askLLM`/`onPluginMessage`) parses a spoken phrase against known route names and jumps to a QR. 12s timeout + fallback message (on-device LLM bridge is known-unreliable).
- Saved routes: add from the wizard ("Save address") or quick-save; persisted in `creationStorage`. One name per route now — no aliases (removed, §9). Save flow re-verified end-to-end — confirming a name now saves immediately, for both pickup-only and destination saves, and `renderHome()` no longer crashes on a pickup-only saved route (§9 — the actual cause of routes "disappearing" from Home).
- Creation icon (`r1/icon.png`) wired into the browser favicon and the install QR's `iconUrl` (§6, §17) — needs the gh-pages publish step to copy it across the first time this ships (see §6's publish reminder).
- "Ride home" on the QR screen: rebuilds a QR to whatever route is named "home"; hidden if none saved (and now guards a Home route with no dropoff instead of crashing, §9).
- MTA subway-map UI: black/Helvetica, colored station bullets, scroll-wheel selection + side-button confirm + swipe (right=back, left=forward, now touch-event-based, §9) + long-press (QR view → Ride home; elsewhere → cancel, now debounced, §9). Transit-line progress bar on the 3 real wizard steps (Pickup/Where/Go), with a sliding, correctly-centered "train dot" (§9).
- Real QR codes render again (see §2 — `qrcode.js` restoration).
- Screen-to-screen slide transitions, selection glow, a road-map/subway-line background texture (upgraded in §9), a redesigned header (§9), and the Home dead-space treatment from §5 — all verified via direct DOM/style inspection and real clicks, including rapid-navigation edge cases.
- Confirm/QR screens show a simplified house-number + street address instead of the full geocode string (§9); "Did you mean" still shows full detail on purpose (helps verify city/state on a fuzzy match).

**NOT done / caveats:**
- **Swipe and long-press hardening (§9) is unverified on real hardware.** These were the two on-device complaints ("swipe doesn't work", "PTT spotty, particularly long-press-to-home") that couldn't be conclusively reproduced or disproven outside the actual R1 — the fixes are defensible engineering (touch events + `touch-action:none` for swipe; debounce + cross-suppression for long-press vs. side-click) but need a real on-device retest to confirm they actually resolve what was reported.
- **GPS is gone, not fixed** — removed the "Current location" option entirely (§9) rather than chase the permission-prompt issue further; "Skip · my location" already gets the same practical outcome via Uber's own location handling.
- On-device voice round-trip not manually tested on real hardware.
- **Nominatim rate limit** (~1 req/s, no key) — fine for personal use, not high volume.
- `setup/` and `backend/` are orphaned (see §2) — kept per request.
- Guest/"for someone else" rides — blocked pending Business API access (§3). (Scheduling isn't in this bucket anymore — it was removed outright in §8 rather than left pending, since no API access would ever fix the public deep link's lack of a schedule parameter.)

## 17. Run / deploy
**The creation is live — no local server needed:**
```
https://travisbrant1-cyber.github.io/uber-routes/
```
Install on the R1: open `install-qr.png` (repo root) and scan it with the R1 camera, OR point the R1's creation loader at the URL above.

**⚠️ The install QR must encode the R1 JSON envelope, NOT a bare URL** — see the format in `install-qr.png`'s generation history if it ever needs regenerating.

**Local dev / edit:**
```
cd C:\Users\travi\Documents\R1-projects\uber-routes
# edit r1/index.html, then:
git add r1/index.html && git commit -m "..." && git push origin master
git checkout gh-pages
git show master:r1/index.html > index.html
git add index.html && git commit -m "publish" && git push origin gh-pages
git checkout master
# GitHub Pages rebuilds in ~30-60s
```
Note: the publish step only ever copies `index.html`, not `qrcode.js` — if `qrcode.js` ever changes on `master`, copy it across too (`git show master:r1/qrcode.js > qrcode.js` while on `gh-pages`).

To inspect the parked guest attempt: `git checkout guest-rides-deeplink` (then `git checkout master` to return).

**For local testing:** `npx serve .` from the repo root, then load `http://localhost:<port>/r1/` **with the trailing slash** — `serve` redirects `/r1/index.html` to the extensionless `/r1` (no trailing slash), which breaks the page's own relative `qrcode.js` reference (resolves against repo root instead of `/r1/`). Not an issue on GitHub Pages, which serves the real URL with a trailing slash. Alternative that sidesteps the gotcha entirely: point `serve` directly at the `r1/` folder as its own server root (e.g. `npx serve r1`) — then `qrcode.js` resolves correctly regardless of trailing slash, since `r1/` itself is the root.

**Boondit emulator (for pre-device testing):** https://creations.boondit.site/devtools → **Emulator** tab. Load the live URL — it renders at 240×282 and dispatches real hardware events via `postMessage`; the creation's built-in bridge shim (top of `<head>`) relays them. The dev-tools page itself is the authoritative source for what events actually exist — confirmed this session it's just the 5 legacy names, nothing else.

## 18. Next steps
- [ ] **Top priority: re-test swipe, long-press-to-Home, and the animated background on the actual R1** (§9, §10) — swipe/long-press couldn't be verified outside a browser at all; the SVG animation's smoothness/CPU cost on the real SoC likewise couldn't be. Confirm swipe reliably fires `goBack()`/`goForward()`, holding the side button to go Home no longer feels "spotty," and the moving background dots don't introduce visible jank.
- [ ] Also re-verify on-device: the pickup-only-route crash fix (save a pickup-only route, then request a ride and return to Home — the route list should still render); typing/saying an address still shows "Did you mean" promptly; the now-3-step wizard (Pickup/Where/Go) feels right with GPS and the When step both gone.
- [ ] If PTT still somehow doesn't register on real hardware: the `#debug` flag (append `#debug` to the URL) logs every `scrollUp/scrollDown/sideClick/longPressStart/longPressEnd/click/touchend/keydown` event with detail to the console — use it to see whether the press is reaching the WebView at all while the on-screen keyboard has focus.
- [ ] Manual on-device test: voice phrase and a real scan-to-Uber ride.
- [ ] Watch Nominatim rate limits under real use; add a tiny client-side cache if needed.
- [ ] Decide on Uber for Business / Guest Rides API — the one path back to real guest rides (§3). No longer tied to scheduling — that was removed outright in §8, not left waiting on this.
- [ ] Optional: delete orphaned `backend/` + `setup/` once confident they're not needed.
- [ ] Nothing from §5 through §15 (visual polish, the save-flow fixes, the new icon, the Enter-key fix, the scheduling removal, the on-device feedback pass, the animated SVG background, the route-preview diagram, its city labels/CSS polish, the muted-accent redesign, the flashing-icon spinner, or route deletion) has been committed/pushed yet — pending user review, same checkpoint as the §4 bug-fix pass.
- [ ] First time this ships: don't forget `icon.png` in the gh-pages publish step (§6, §17) or the install QR's `iconUrl` will 404.
- [ ] Deleting saved pickups/places from the Pickup screen and Where's "Saved place" picker (§15) is still not possible — only Home got the hold-to-delete gesture, deliberately, to avoid clashing with those screens' existing long-press-to-Home behavior. Worth a follow-up if it turns out to matter.
- [ ] Optional: the scanline-overlay suggestion from §12's CSS review was skipped over QR-scannability risk — revisit if still wanted, with a QR-screen exclusion built in.
- [ ] Optional, not part of any request so far: `themeColor` in the install QR envelope is still `#FE5000`, a holdover that doesn't match the icon's green or the app's black/yellow chrome — worth revisiting whenever the icon/branding gets another look.
- [ ] Update `HANDOFF.md` (this file) whenever the architecture changes again.
